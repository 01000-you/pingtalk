# 배포 준비 완료 요약

## ✅ 완료된 작업

### 1. PackageName 변경
- ✅ `com.example.mobile` → `com.pingtalk.app`으로 변경 완료
- ✅ 모든 관련 파일 및 디렉토리 구조 업데이트 완료

### 2. 앱 이름 변경
- ✅ AndroidManifest.xml: "mobile" → "PingTalk"
- ✅ pubspec.yaml: 앱 설명 추가

### 3. 릴리즈 서명 설정
- ✅ build.gradle.kts에 Keystore 서명 설정 추가 완료
- ✅ key.properties 파일 생성 완료 (비밀번호 입력 필요)
- ✅ Keystore 생성 스크립트 준비 완료

## 📋 다음 단계 (필수)

### 1. Keystore 생성 (중요!)

다음 명령어를 실행하여 Keystore를 생성하세요:

```bash
cd mobile/android
./generate_keystore.sh
```

또는 수동으로 생성:

```bash
cd mobile/android
keytool -genkey -v -keystore pingtalk-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias pingtalk
```

**⚠️ 중요:**
- 생성된 Keystore 파일(`pingtalk-release-key.jks`)과 비밀번호를 **안전한 곳에 백업**하세요
- 분실 시 앱 업데이트가 불가능합니다
- 이 파일들은 `.gitignore`에 포함되어 있어 Git에 커밋되지 않습니다

### 2. key.properties 파일 업데이트

Keystore 생성 후, `mobile/android/key.properties` 파일을 열어서 실제 비밀번호를 입력하세요:

```properties
storePassword=실제_keystore_비밀번호
keyPassword=실제_키_비밀번호
keyAlias=pingtalk
storeFile=pingtalk-release-key.jks
```

### 3. 릴리즈 빌드 테스트

Keystore 설정이 완료되면 빌드를 테스트하세요:

```bash
cd mobile
flutter build appbundle --release
```

빌드가 성공하면 다음 파일이 생성됩니다:
- `mobile/build/app/outputs/bundle/release/app-release.aab` (모바일 앱)
- `mobile/android/wear/build/outputs/apk/release/wear-release.apk` (워치 앱)

워치 APK는 별도로 빌드:

```bash
cd mobile/android
./gradlew :wear:assembleRelease
```

### 4. Play Console 준비

배포 가이드(`docs/07-구글플레이스토어-배포가이드.md`)를 참고하여:

1. **Play Console 계정 생성** (개발자 등록 $25)
2. **앱 등록 및 기본 정보 입력**
3. **스토어 리스팅 준비**
   - 앱 아이콘 (512x512px)
   - 피처 그래픽 (1024x500px)
   - 스크린샷 (최소 2개)
   - 앱 설명
4. **개인정보처리방침 URL 준비**
5. **앱 번들 및 Wear OS APK 업로드**

## 📁 생성된 파일

- `mobile/android/generate_keystore.sh` - Keystore 생성 스크립트
- `mobile/android/key.properties` - Keystore 설정 파일 (비밀번호 입력 필요)
- `mobile/android/key.properties.example` - 설정 파일 템플릿
- `mobile/android/KEYSTORE_생성_가이드.md` - 상세 가이드

## 🔒 보안 주의사항

- ⚠️ `key.properties` 파일과 `pingtalk-release-key.jks` 파일은 **절대 Git에 커밋하지 마세요**
- ⚠️ Keystore 비밀번호를 안전하게 보관하세요
- ⚠️ 여러 곳에 백업하는 것을 권장합니다

## 📚 참고 문서

- [구글 플레이 스토어 배포 가이드](../docs/07-구글플레이스토어-배포가이드.md)
- [Keystore 생성 가이드](./KEYSTORE_생성_가이드.md)

